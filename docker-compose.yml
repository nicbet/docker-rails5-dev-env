---
version: '2'
services:
  app:
    build: .
    ports:
      - "3001:3000"
    volumes:
      - .:/app
    volumes_from:
      - gems
    depends_on:
      - db
  db:
    image: postgres:9
  gems:
    image: busybox
    volumes:
      - /ruby_gems
